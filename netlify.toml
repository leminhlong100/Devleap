[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: mọi route đều trả về index.html để vue-router (history mode) xử lý
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
